function kalku() {
    let rase;
    rase = (8 / 2) + (3 * 5) - 4;
    console.log(rase);
}

function kalku() {
    let rase;
    rase = (8 / 2) + (3 * 5) - 4;
    console.log(rase);
}   
kalku(); // Panggil fungsi untuk menampilkan hasil

